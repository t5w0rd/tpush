apiVersion: v1
kind: Service
metadata:
  name: push
  namespace: default
  labels:
    name: push-srv
    micro: service
spec:
  ports:
  - name: http
    port: 80
    targetPort: 18080
  selector:
    name: push-srv
    micro: service
  type: LoadBalancer
